kw1-SELECT nazwisko,imie,klasa,data_wypozyczenia FROM `uczniowie` WHERE data_wypozyczenia > '2020-03-02';

kw2-SELECT uczniowie.nazwisko,uczniowie.imie,uczniowie.data_wypozyczenia,ksiazki.kod,ksiazki.autor FROM ksiazki JOIN uczniowie ON ksiazki.id = uczniowie.id_ksiazki WHERE 1;

kw3-DELETE FROM `ksiazki` WHERE autor = '0';

kw4-SELECT autor, tytul, kod FROM ksiazki ORDER BY rand() LIMIT 5;